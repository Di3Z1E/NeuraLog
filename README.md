# NeuraLog
NeuraLog is a lightweight Kubernetes-native log aggregator that intelligently discovers running pods across the cluster and streams their logs in real-time to a centralized NFS storage. It combines dynamic tracking, automated 7-day retention cleanup, and minimal resource usage all without intrusive sidecars.
