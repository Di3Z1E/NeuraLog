apiVersion: v1
kind: Service
metadata:
  name: {{ include "neuralog.fullname" . }}
  namespace: {{ include "neuralog.namespace" . }}
  labels:
    {{- include "neuralog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "neuralog.selectorLabels" . | nindent 4 }}
