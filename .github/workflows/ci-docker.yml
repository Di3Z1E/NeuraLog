name: CI / Docker

# Build-only check on PRs — verifies Dockerfiles stay buildable.
# No push. Actual image publishing is handled by release.yml.

on:
  pull_request:
    branches: [main]
    paths:
      - 'collector/**'
      - 'ui/**'
      - 'docker/**'
      - '.github/workflows/ci-docker.yml'

jobs:
  build-collector:
    name: Build Collector Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Build (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/collector.Dockerfile
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-ui:
    name: Build UI Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Build (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/ui.Dockerfile
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
